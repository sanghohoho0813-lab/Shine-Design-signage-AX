# 선택 — 비워두면 이벤트는 브라우저(localStorage)에만 쌓인다.
# 값을 넣으면 같은 이벤트를 navigator.sendBeacon 으로도 보낸다 (GA4 Measurement Protocol / PostHog / Supabase Edge Function).
NEXT_PUBLIC_ANALYTICS_ENDPOINT=

# 배포 URL — sitemap / OG 절대경로에 쓰인다
NEXT_PUBLIC_SITE_URL=https://shine-design-signage-ax.vercel.app
