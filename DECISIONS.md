
# DECISIONS

0-ad. **향후 확장 메뉴 확대(v11)** — 고객 4개 → **10개**, AX에는 **9개**를 새로 만들었다.
  - **근거 없는 확장은 넣지 않는다.** 각 항목에 `basis`(확장 근거)를 필수로 달고, 지명원 실적·품목·자격에서 확인되는 것만 올렸다. BF 인증 컨설팅은 BF 7품목 자체 보유, CI 교체 전국 대응은 도로교통공단 CI 변경 현황조사 용역 실수행 + 전국 57건, 주물 현판·조형물은 주물현판 20여 건, 전시·홍보관은 국립항공박물관·서울시립과학관 실적, 기업·브랜드는 삼성전자·넥센타이어·쿠팡 실적이 근거다. 시트를 열면 이 근거가 먼저 보인다.
  - **AX 확장은 "지금 시스템 밖에서 처리되는 구간"을 골랐다.** 현장 실측·사진(카톡), 설치 일정·배차(수첩), 자재·재고(공장 전화), 정산·세금계산서(견적 이후가 비어 있음), 도면 버전, 하자·A/S, 나라장터 공고, 협력사 단가, 경영 리포트. 있으면 좋은 기능이 아니라 이미 존재하는 업무의 빈칸이다.
  - **3단계 티어** — NEXT(다음 단계) / Preview(검토 중) / Long-term(장기). 배지만 다르게 하지 않고 시트에 도입 시점 문구를 함께 넣어 기대치를 맞춘다.
  - **AX 사이드바는 기본 접힘** — v1.4의 현재:향후 비중 규칙을 지키기 위해, 확장 9개는 접힌 상태로 두고 헤더에 "9개 · NEXT 4"만 표시한다. 실제 모듈 9개가 스크롤 밖으로 밀리지 않는다.
  - 시트·행·배지는 `components/FutureSheet.tsx` 하나로 통일해 고객 화면과 AX가 같은 컴포넌트를 쓴다. ⌘K 검색에서도 찾히며, 선택하면 404 대신 미리보기 시트가 열린다.
  - 사이드바 nav에 `min-h-0 overflow-y-auto`를 넣었다. 메뉴가 길어지면 하단의 시연 모드·고객 사이트 링크가 화면 밖으로 밀려나던 문제를 함께 고쳤다.

0-ab. **지명원 2026 전면 반영(v10)** — 새 「(주)샤인디자인 지명원_2026」을 1차 출처로 삼아 회사 정보를 다시 썼다.
  - **연락처 공개** — 지명원 표지에 회사가 직접 공개한 TEL 02-963-9493 / FAX 031-575-8411 / shine9493@naver.com. 이전 라운드에서 "확인된 공개 연락처가 없어 임의로 만들지 않는다"(0-y)고 판단해 미기재했던 것을, 근거 자료가 생겼으므로 게재로 전환한다. 문의 양식은 그대로 두고 전화·이메일을 병행 채널로 추가.
  - **사업장 정정** — 종전 "화성 제1공장 + 남양주 제2공장" 4개 사업장 표기는 근거가 약했다. 지명원 판권면 기준 본사(남양주) · 서울사무소(송파 문정) · 공장(화성 마도) 3곳으로 바로잡고, 관련 카피(홈·FAQ·Why AX·제작 관리·시연 모드)를 모두 수정했다.
  - **자격 6종 → 8종** — 직접생산확인증명서와 중소기업 확인서를 추가하고 발급기관을 함께 표기. 직접생산확인 등재 7품목(간판 및 광고물 제조업 외)은 조달에서 "직접 만들 수 있는가"를 증명하는 근거라 별도 블록으로 노출하고, AX 입찰 서류 체크리스트에도 넣었다.
  - **연혁 정정** — 2023.03 디자인 부문 분리 단계가 빠져 있었다. 2005 샤이니스 → 2023 분리 → 2024 법인 등록으로 실제 경로를 복원.
  - **조직도 · 사인 카탈로그 신설** — 지명원의 WORK ORGANIZATION과 WORK SCOPE를 화면으로 옮겼다. 카탈로그 48품목 중 BF(장애물 없는 생활환경) 사인 7품목은 공공시설 필수 항목이라 별도 강조 카드로 분리하고 FAQ 문항도 추가했다.
0-ac. **전체 수행 실적 337건(v10)** — 지명원 기업실적(2013~2025)을 `lib/records.ts`에 옮기고 `/portfolio#records`에 검색 화면을 만들었다. 발주 담당자가 "우리 같은 기관을 해봤나"를 확인하는 것이 실제 용도라, 키워드 검색·일치 부분 하이라이트·자주 찾는 검색 칩·기간 필터·발주처 성격별 집계를 붙였다. 홈 통계 밴드와 Why AX 수치도 이 데이터에서 계산하므로 실적이 늘면 자동으로 갱신된다. AX 증빙 인쇄물에도 전체 표를 추가해 제출자료로 바로 쓸 수 있다.
  - **연도 표기** — 지명원 실적 페이지는 연도 라벨이 좌측 열에 모여 있고 항목은 우측에 이어지는 레이아웃이라 개별 건의 연도를 단정할 수 없다. 없는 정보를 만들어 붙이는 대신 원문 페이지 단위 기간(2024–2025 / 2021–2023 / 2016–2020 / 2013–2015)으로 묶었다.
  - **분야 집계는 중복 없이** — 한 실적이 여러 분야 정규식에 걸리면 합계가 전체를 넘어 오해를 부른다. 구체적인 분야부터 먼저 매칭해 건당 한 분야에만 넣는다.
  - 계약금액은 지명원에 없으므로 표기하지 않는다(0-a 원칙 유지).

0-aa. **메뉴 목차 분류(v9)** — 평평하게 나열되던 AX 9개 메뉴를 업무 흐름대로 4그룹(오늘 / 프로젝트 운영 / 성장·자산 / 시스템)으로 묶고 각 그룹에 한 줄 힌트를 붙였다. 정의는 `components/ax/menu.ts` 한 곳에 두어 사이드바·모바일 Drawer·전역 검색(⌘K)이 같은 분류를 공유한다. 고객 Drawer도 회사 / 실적·과정 / 문의 / 향후 확장으로 나눴다.

0-w. **화면 전환 연출(v8)** — 메뉴를 누르면 새 화면이 즉시 튀어나오지 않고 `130ms` 텀 뒤 `380ms`에 걸쳐 부드럽게 나타난다(`page-in`, cubic-bezier(.22,.61,.36,1)). 그 공백은 상단 진행 바가 메운다. Motion 줄이기·`prefers-reduced-motion`에서는 애니메이션 없이 즉시 표시.
0-x. **Why AX 섹션 네비(v8)** — 12섹션 긴 글에 스크롤 스파이 기반 섹션 바로가기와 읽기 진행률을 붙였다. sticky 위치는 상단바 실제 높이(`--ax-topbar-h`, ResizeObserver로 측정)를 따라가므로 글자 확대·2줄 줄바꿈에도 가려지지 않는다.
0-y. **FAQ / 회사 정보(v8)** — 공공 발주 담당자가 먼저 묻는 6가지(디자인만 의뢰, 전국 시공, 운영 중 교체, 보유 자격, 기간, 예산 미확정)를 문의 페이지 하단에 배치. 회사소개에는 사업자 정보와 4개 사업장 지도 링크를 추가하고 Organization JSON-LD를 넣었다. 전화·이메일은 확인된 공개 연락처가 없어 임의로 만들지 않고 문의 양식으로 안내한다.
0-z. **오늘 할 일(v8)** — 대시보드에서 리스크·검수대기·신규문의·Margin 미달·입찰 마감을 자동으로 모아 체크리스트로 제시하고, 체크 상태를 저장한다. AX 로딩은 텍스트 대신 레이아웃 스켈레톤으로 교체.

0-s. **⌘K 전역 검색(v7)** — 21건 포트폴리오·9개 AX 모듈을 클릭으로 찾아다니던 문제. 어디서든 Cmd/Ctrl+K로 메뉴·프로젝트·포트폴리오·실행(시연/테마)을 검색해 이동한다. 역할(role)에 따라 AX 항목은 숨긴다.
0-t. **문의 초안 자동저장(v7)** — 5단계 위저드에서 새로고침 시 입력이 전부 사라지던 문제. localStorage 초안 저장 + "이어서 작성" 배너 + 단계 칩 클릭으로 되돌아가 수정. 단계 이동 시 폼 상단으로 스크롤.
0-u. **사이트 기본기(v7)** — metadataBase/OG 이미지(동적 생성)/페이지별 메타·sitemap.xml·robots.txt(AX 색인 제외)·favicon·error.tsx·loading 스켈레톤·본문 바로가기 링크. 링크 공유 시 빈 카드로 보이던 문제 해결.
0-v. **증빙 인쇄 리포트(v7)** — 완료 실적 + 포트폴리오를 표로 묶은 인쇄 전용 영역(`@media print`)을 두어 브라우저 인쇄/PDF 저장으로 제출자료를 만들 수 있게 했다.

0-n. **디자인 시스템 정립(v6)** — 페이지마다 제각각이던 타이포·간격·사진 처리를 하나의 규칙으로 통합.
  - Type ramp: `.t-display/.t-h1/.t-h2/.t-h3/.t-lead/.t-body/.t-meta/.t-eyebrow` + `.measure`(가독 폭)
  - Layout: `.container-page` / `.section` / `.section-sm` 로 섹션 리듬 고정
  - Photo: `.scrim-hero/.scrim-band/.scrim-card` — 사진 위 텍스트는 반드시 정의된 스크림 위에만
  - Button: `.btn` + primary/accent/ghost/on-dark 4역할만 사용
  - 프리미티브 컴포넌트 `Section` / `SectionHeader` / `PhotoBand` (components/ui.tsx)
0-o. **한국어 조판(v6)** — `word-break: keep-all`로 어절 중간 줄바꿈 제거("완성합니/다" 같은 깨짐), 제목 `text-wrap: balance`, 본문 `text-wrap: pretty`. 고객 화면이 "불규칙하다"는 인상의 주원인이었다.
0-p. **cascade layer 이슈(v6)** — Tailwind v4에서 레이어 밖 CSS는 유틸리티보다 항상 우선하므로, 디자인 시스템 클래스를 `@layer components`에 넣어 `text-*` 등 유틸리티로 덮어쓸 수 있게 했다. (어두운 배경에서 본문이 어두운 색으로 고정되던 버그의 원인)
0-q. **홈 재구성(v6)** — 신뢰 카드가 사진 위 제목 + 사진 밖 설명으로 이중 표기되어 어수선했던 것을 3×2 그리드 + 하단 스크림 단일 제목 구조로 정리. Hero는 밝은 그라디언트 대신 다크 스크림으로 통일해 사진과 텍스트가 한 덩어리로 읽히게 했다. 이모지 아이콘은 선 아이콘으로 교체.
0-r. **AX 직관성(v6)** — 모듈마다 `PageHeader`(제목 + 이 화면의 목적 한 줄 + 현황 배지)를 두어 처음 들어온 사람도 무엇을 하는 화면인지 즉시 알 수 있게 했다.

0-k. **텍스트 크기 전면 rem화(v5)** — 사이드바 메뉴 등 133곳이 `text-[Npx]` 고정값이라 Font Scale에 반응하지 않던 문제를 rem 기반으로 일괄 전환. 이제 모든 텍스트가 설정에 따라 함께 확대된다.
0-l. **스케일 재조정(v5)** — 기본 1.22 / 크게 1.45 / 매우 크게 1.70 (종전 1.1/1.22/1.35). 기본을 다시 1.1배 올리고 단계 간 차이를 뚜렷하게 했다. 설정 화면에 실시간 미리보기 추가.
0-m. **확대 대응 레이아웃(v5)** — 사이드바 폭 `clamp(280px,17.5rem,360px)`, AX 상단바 가변 높이+줄바꿈, KPI·보드 그리드를 `auto-fit minmax(rem)`로 전환해 글자가 커지면 열 수가 자동으로 줄어든다. 고객 헤더의 인라인 메뉴는 크게/매우 크게에서 서랍으로 이동(`[data-font]` 기준).

0-g. **파이프라인 리스트 우선(v4)** — 8컬럼 칸반의 좌우 스크롤 비효율 지적에 따라 기본 뷰를 납기순 리스트(단계 배지·D-day·리스크 인셋 바)로 전환, 보드는 화면폭 안에 맞는 압축 그리드로 토글 제공. 단계 칩=필터.
0-h. **견적·원가 컬러 시스템(v4)** — 원가 7개 항목에 dataviz 검증 통과 고정 팔레트 적용(스택 바+범례+상세 행, 세그먼트 2px 갭). Margin은 양호/주의/위험 3색 배지. 데모 원가를 재조정해 3색이 섞이도록 보정.
0-i. **폰트 스케일 재정의(v4)** — 기본 크기를 1.1배로 상향하고 단계를 기본(1.1)/크게(1.22)/매우 크게(1.35)로 재구성. 구버전 저장값은 기본으로 마이그레이션.
0-j. **문의 브릿지 강화(v4)** — 파이프라인 상세에서 고객 문의 원본(담당자·연락처·예산·일정) 표시 및 응대 상태(접수/검토중/상담예약) 변경 가능.

0-e. **테마 7종 확장(v3)** — 사용자의 명시적 지시("색상 7개 이상 조합")에 따라 v1.2의 "6개 유지" 규칙을 오버라이드(Instruction Priority 1순위). 라이브러리 6종 전체(Graphite Copper 복원) + Shine 브랜드 테마 = 7종. 각 테마 6토큰 구조는 유지.
0-f. **UI/UX 고도화(v3)** — 스크롤 리빌(+reduced-motion 무효화), 홈 통계 밴드(CountUp), KPI 스파크라인(단일 시리즈·축 없음), 포트폴리오 호버 오버레이·필터 카운트·갤러리 라이트박스, 문의 위저드 요약 확인, 이벤트 토스트, 헤더 스크롤 섀도, Back-to-top, focus-visible 링.

0-a. **실명 포트폴리오 전환(v2)** — 회사소개서(공개 홍보자료)에 수록된 실제 프로젝트·실제 사진이 제공됨에 따라, 중립 표기를 실명 표기(한국도로교통공단 강서운전면허시험장 등)로 전환. Master Prompt §22 "실제 자료 우선" 지시 이행. 계약금액은 여전히 미기재.
0-b. **AX 데모 데이터도 실제 발주처 기반(v2)** — 몰입감을 위해 파이프라인·입찰 데모를 실제 거래처(도로교통공단·부천성모병원·보령시 등) 기반의 가상 진행 건으로 구성, DEMO 배지 유지.
0-c. **제조 표현(v2)** — 회사소개서의 장비보유현황(1·2공장 가공·조립 라인) 확인에 따라 "자체 1·2공장 + 전문 파트너 네트워크"로 표현 조정 (과장 없이 실보유 설비 기준).
0-d. **시연 모드 추가(v2)** — Unified §15 Presentation Mode 요구 반영. 루트 레이아웃 마운트로 Customer↔AX 라우트 전환에도 유지되는 10단계 데모.

1. **Theme 기본값** — v1.1 라이브러리 6종 중 브랜드 지시(그래파이트+골드)에 맞춰 1번 슬롯을 "Shine Graphite Gold"로 커스텀, 나머지 5종(Navy/Teal/Burgundy/Indigo/Forest)은 라이브러리 그대로. 총 6종 유지(7번째 추가 금지 규칙 준수).
2. **포트폴리오 실명 표기** — Master Prompt §8/§22의 "실기관 프로젝트 날조 금지"에 따라, 신뢰 섹션은 프롬프트가 명시한 카테고리 카드(한국도로교통공단 등)를 사용하되 포트폴리오 상세는 "도로교통 분야 공공기관" 등 중립 표기 + 계약금액 미기재.
3. **AX 데모 데이터** — 참조 시안의 기관명/수치는 DEMO 배지와 함께 사용. 금액·일정은 모두 데모 값임을 화면에 명시.
4. **AI 표기 정직성** — 규칙 기반 인사이트는 모두 "AI READY — 규칙 기반 Demo"로 표시(§8 AI Policy).
5. **상태 관리** — 외부 DB 없이 React Context + localStorage. 교체 지점(CSV/Supabase/API)을 설정 화면에 명시.
6. **Device Preview** — 별도 Route 대신 현재 Route를 same-origin iframe으로 렌더(404 위험 0, 상태 공유). embedded 감지로 재귀 차단.
7. **OEM 표현** — "외주 의존" 대신 "제작 파트너 관리 / 유연한 공급구조"로 일관 표기(Master Prompt §11, §16).
8. **미사용 AX 자산 4종** — v1.4 AX 시각 절제(30~50%) 규칙 준수를 위해 의도적으로 미배치.
9. **styled-jsx** — 문의 위저드 입력 스타일만 사용, 나머지는 Tailwind 토큰 유틸.
